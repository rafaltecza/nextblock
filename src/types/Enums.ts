export enum StyleType {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    DEFAULT = 'default',
}

export enum IconSide {
    LEFT = 'left',
    RIGHT = 'right',
}

export enum AnimationEvent {
    ALWAYS = "ALWAYS",
    HOVER = "HOVER",
    FOCUS = "FOCUS",
}

export enum AnimationType {
    BOUNCE = "BOUNCE",
    ROTATE = "ROTATE",
    FADE = "FADE",
}
